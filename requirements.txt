# Core deep learning
torch>=2.0.0
torchvision>=0.15.0

# Diffusion models & transformers (for synthetic data generation)
diffusers>=0.27.0
transformers>=4.40.0
accelerate>=0.30.0

# Background removal
rembg[gpu]>=2.0.76        # Use rembg[cpu] if no GPU available

# Image processing & augmentation
Pillow>=10.0.0
albumentations>=1.3.0
numpy>=1.24.0

# Object detection
ultralytics>=8.0.0

# Utilities (pulled in automatically, pinned for reproducibility)
safetensors>=0.4.0
huggingface-hub>=0.20.0
